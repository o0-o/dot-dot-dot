# ZPROFILE — login-shell environment; shared config lives in ../sh
# (ZDOTDIR is set by ~/.zshenv)

for profile in "${ZDOTDIR}/../sh/profile.d/"*.sh; do
  source "${profile}" ||
  printf 'ZPROFILE: An error occured in %s\n' "${profile##*/}" >&2
done
unset profile
